# Maps repository structure into a graph (skeleton)
walker RepoMapper {
    can map with `root entry {
        # TODO: Map repository structure into nodes/edges
        # e.g., Repository -> Directory -> CodeFile
    }
}
